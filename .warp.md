# Warp Project Rules (.warp.md)

## Rules
- 本地开发数据库使用 Docker 的 Postgres。
- Jenkins 构建使用 Docker-based agent。
- 生产环境数据使用 Postgres。
- 所有开发工作需绑定任务系统中的某个任务（task_id）。
- 不要总问一些不重要的功能点开发，优先采用 MVP 思想，尽量减少打扰。

## Task Context
- 当前任务 ID 优先从环境变量 `WARP_TASK_ID` 读取；若未设置则从 `.warp/state.json` 的 `current_task_id` 读取。
- 推荐通过 `scripts/task.sh` 管理当前任务 ID。
