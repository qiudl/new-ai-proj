# Task 307: 任务文档上传下载功能 - 开发计划与子任务分解

## 📋 项目概述

**父任务**: Task 266 - 32周-02：任务管理优化  
**主任务**: Task 307 - 任务文档支持手工和接口上传，md和pdf格式下载  
**预估总工时**: 64小时  
**开发周期**: 16个工作日 (每日4小时)  
**优先级**: 高

## 🎯 项目目标

实现完整的任务文档管理系统，支持：
- 多种方式的文档上传（手工+API）
- 多种格式的文档下载（Markdown+PDF）
- 完整的版本控制和操作审计
- 用户友好的前端界面
- 企业级的安全和性能保障

## 📅 开发阶段规划

### 阶段1: 基础架构设计 (8小时, 2天)
- 数据库设计和迁移
- API接口设计
- 文件存储架构设计

### 阶段2: 后端核心功能 (24小时, 6天)
- 文档服务开发
- 上传下载处理器
- API端点实现

### 阶段3: 前端用户界面 (20小时, 5天)
- React组件开发
- 状态管理
- 用户交互优化

### 阶段4: 集成测试优化 (8小时, 2天)
- 功能集成测试
- 性能优化
- 错误处理完善

### 阶段5: 部署和文档 (4小时, 1天)
- 生产环境部署
- 用户文档编写
- 维护指南制作

## 🏗️ 子任务分解

### 阶段1子任务: 基础架构设计

#### 子任务307-01: 数据库表结构设计
**预估工时**: 3小时  
**优先级**: 高  
**依赖**: 无

**描述**: 设计文档管理所需的数据库表结构，包括文档信息表、版本表、操作日志表。

**验收标准**:
- 完成task_documents主表设计
- 完成document_versions版本表设计  
- 完成document_logs日志表设计
- 包含完整的索引和约束
- 提供UP和DOWN迁移脚本

---

#### 子任务307-02: 文件存储架构设计
**预估工时**: 2小时  
**优先级**: 高  
**依赖**: 307-01

**描述**: 设计文件存储的目录结构、命名规则、路径生成策略。

**验收标准**:
- 定义文件存储目录结构
- 设计文件命名规则（防冲突）
- 制定路径生成和查找策略
- 考虑文件清理和备份机制

---

#### 子任务307-03: API接口规范设计
**预估工时**: 3小时  
**优先级**: 高  
**依赖**: 307-01, 307-02

**描述**: 设计RESTful API接口规范，包括请求参数、响应格式、错误处理。

**验收标准**:
- 完成5个核心API接口设计
- 定义请求和响应数据结构
- 制定错误码和处理规范
- 编写API文档和示例

---

### 阶段2子任务: 后端核心功能

#### 子任务307-04: 数据库迁移实现
**预估工时**: 2小时  
**优先级**: 高  
**依赖**: 307-01

**描述**: 实现数据库迁移脚本，创建所需的表结构、索引、触发器。

**验收标准**:
- 编写完整的UP迁移脚本
- 编写对应的DOWN回滚脚本
- 包含表、索引、触发器、权限设置
- 通过迁移测试验证

---

#### 子任务307-05: 文档服务层开发
**预估工时**: 8小时  
**优先级**: 高  
**依赖**: 307-04

**描述**: 开发核心的文档服务，包括文件验证、存储、检索、版本管理。

**验收标准**:
- 实现文件上传和存储功能
- 实现文件验证（类型、大小、内容）
- 实现版本控制和历史管理
- 实现文件完整性检查（checksum）
- 包含完整的错误处理

---

#### 子任务307-06: 手工上传处理器开发
**预估工时**: 4小时  
**优先级**: 高  
**依赖**: 307-05

**描述**: 实现手工上传的HTTP处理器，支持多文件上传、进度跟踪。

**验收标准**:
- 支持multipart/form-data格式
- 实现文件上传进度跟踪
- 支持批量文件上传
- 完整的参数验证和错误处理
- 包含操作日志记录

---

#### 子任务307-07: API上传处理器开发
**预估工时**: 4小时  
**优先级**: 高  
**依赖**: 307-05

**描述**: 实现API上传的处理器，支持base64内容上传、程序化调用。

**验收标准**:
- 支持JSON格式的API调用
- 实现base64内容解码和验证
- 支持文件元数据设置
- 完整的API认证和授权
- 包含详细的操作审计

---

#### 子任务307-08: 文档下载处理器开发
**预估工时**: 6小时  
**优先级**: 高  
**依赖**: 307-05

**描述**: 实现文档下载处理器，支持Markdown和PDF格式导出。

**验收标准**:
- 实现Markdown格式导出
- 实现PDF格式生成和导出
- 支持文档模板和样式定制
- 包含附件文档的整合导出
- 支持批量下载和压缩

---

### 阶段3子任务: 前端用户界面

#### 子任务307-09: 任务文档服务扩展
**预估工时**: 3小时  
**优先级**: 高  
**依赖**: 307-06, 307-07, 307-08

**描述**: 扩展前端的taskDocumentService，添加新的上传下载方法。

**验收标准**:
- 扩展现有service添加新方法
- 实现文件上传的进度回调
- 实现批量上传功能
- 添加完整的TypeScript类型定义
- 包含错误处理和重试机制

---

#### 子任务307-10: 文档上传组件开发
**预估工时**: 6小时  
**优先级**: 高  
**依赖**: 307-09

**描述**: 开发TaskDocumentUploader组件，提供拖拽上传、进度显示等功能。

**验收标准**:
- 实现拖拽上传界面
- 支持文件选择和预览
- 实现上传进度显示
- 支持批量上传操作
- 包含文件验证和错误提示
- 响应式设计和移动端适配

---

#### 子任务307-11: 文档管理组件开发
**预估工时**: 6小时  
**优先级**: 高  
**依赖**: 307-09

**描述**: 开发TaskDocumentManager组件，提供完整的文档管理界面。

**验收标准**:
- 实现文档列表展示
- 支持文档搜索和筛选
- 实现文档操作（下载、删除、重命名）
- 显示文档统计信息
- 支持版本历史查看
- 集成上传和下载功能

---

#### 子任务307-12: 文档小部件组件开发
**预估工时**: 3小时  
**优先级**: 中  
**依赖**: 307-10, 307-11

**描述**: 开发TaskDocumentWidget轻量级组件，用于任务卡片和详情页集成。

**验收标准**:
- 实现紧凑型显示模式
- 支持快速上传和下载
- 显示文档统计徽章
- 支持一键打开管理器
- 适配不同的使用场景

---

#### 子任务307-13: 文档管理Hook开发
**预估工时**: 2小时  
**优先级**: 中  
**依赖**: 307-09

**描述**: 开发useTaskDocuments自定义Hook，提供状态管理和操作方法。

**验收标准**:
- 实现文档列表状态管理
- 提供上传下载操作方法
- 包含加载状态和错误处理
- 支持数据缓存和刷新
- 提供统计和过滤工具方法

---

### 阶段4子任务: 集成测试优化

#### 子任务307-14: 功能集成测试
**预估工时**: 4小时  
**优先级**: 高  
**依赴**: 307-10, 307-11, 307-12, 307-13

**描述**: 进行完整的功能集成测试，验证各组件间的协作。

**验收标准**:
- 完成端到端功能测试
- 验证文件上传下载流程
- 测试错误处理和边界情况
- 验证用户权限和安全控制
- 测试不同浏览器的兼容性

---

#### 子任务307-15: 性能优化和错误处理
**预估工时**: 4小时  
**优先级**: 中  
**依赖**: 307-14

**描述**: 优化系统性能，完善错误处理机制，提升用户体验。

**验收标准**:
- 优化大文件上传性能
- 实现文件上传的断点续传
- 优化文档列表加载性能
- 完善错误提示和用户引导
- 添加操作确认和防误操作

---

### 阶段5子任务: 部署和文档

#### 子任务307-16: 生产环境部署和文档编写
**预估工时**: 4小时  
**优先级**: 中  
**依赖**: 307-15

**描述**: 准备生产环境部署，编写用户文档和维护指南。

**验收标准**:
- 编写部署指南和配置说明
- 编写用户使用文档
- 编写API文档和集成示例
- 准备故障排除指南
- 完成功能演示和培训材料

## 🎯 各阶段Prompts

### 阶段1 Prompts: 基础架构设计

#### Prompt 307-01: 数据库表结构设计
```
请为任务文档管理系统设计完整的数据库表结构。需要包含以下要求：

1. **task_documents主表**：
   - 存储文档基本信息：文件名、原始名称、路径、大小、MIME类型
   - 记录上传信息：上传者、上传时间、上传方式（manual/api）
   - 版本控制：版本号、是否激活状态
   - 数据完整性：文件校验和、元数据（JSONB格式）

2. **document_versions版本表**：
   - 记录文档的历史版本
   - 版本号、文件路径、大小、校验和
   - 创建者、创建时间、变更说明

3. **document_logs操作日志表**：
   - 记录所有文档操作：上传、下载、删除、查看
   - 操作者、操作时间、IP地址、User-Agent
   - 操作结果、错误信息

请提供：
- 完整的CREATE TABLE语句
- 合适的索引策略
- 外键约束和检查约束
- 触发器（自动更新时间戳、版本控制）
- UP迁移和DOWN回滚脚本

技术要求：PostgreSQL 12+，支持JSONB，考虑查询性能。
```

#### Prompt 307-02: 文件存储架构设计
```
请设计任务文档管理系统的文件存储架构，需要满足以下要求：

1. **目录结构设计**：
   - 按日期分层存储：/uploads/documents/tasks/YYYY/MM/DD/
   - 支持项目和任务的层次结构
   - 考虑文件数量增长和查找效率

2. **文件命名策略**：
   - 防止文件名冲突
   - 保留原始文件名信息
   - 支持版本管理
   - 便于系统维护和清理

3. **路径生成算法**：
   - Go语言实现
   - 根据任务ID、时间戳、随机数生成唯一路径
   - 支持相对路径和绝对路径转换

4. **存储管理策略**：
   - 文件清理和归档机制
   - 存储空间监控
   - 备份和恢复策略

请提供：
- 详细的目录结构说明
- 文件命名规则和示例
- Go代码实现的路径生成函数
- 存储管理的最佳实践建议

考虑因素：高并发访问、存储效率、维护便利性。
```

#### Prompt 307-03: API接口规范设计
```
请设计任务文档管理系统的RESTful API接口规范，包含以下5个核心接口：

1. **手工上传接口**：
   - POST /api/v1/projects/:projectId/tasks/:taskId/upload
   - 支持multipart/form-data格式
   - 支持单文件和多文件上传
   - 返回上传结果和文档信息

2. **API上传接口**：
   - POST /api/v1/projects/:projectId/tasks/:taskId/upload-api
   - 支持JSON格式，base64编码内容
   - 支持文件元数据设置
   - 适合程序化调用

3. **文档列表接口**：
   - GET /api/v1/projects/:projectId/tasks/:taskId/uploads
   - 支持分页、排序、筛选
   - 返回文档列表和统计信息

4. **Markdown下载接口**：
   - GET /api/v1/projects/:projectId/tasks/:taskId/download/md
   - 整合任务文档和附件
   - 返回格式化的Markdown内容

5. **PDF下载接口**：
   - GET /api/v1/projects/:projectId/tasks/:taskId/download/pdf
   - 将Markdown转换为PDF格式
   - 支持自定义样式和模板

请提供：
- 详细的接口定义（HTTP方法、路径、参数）
- 请求和响应数据结构（JSON Schema）
- 错误码定义和处理规范
- 认证授权要求
- 接口使用示例和集成指南

技术要求：RESTful设计、JWT认证、统一错误处理。
```

### 阶段2 Prompts: 后端核心功能

#### Prompt 307-04: 数据库迁移实现
```
基于已设计的数据库表结构，请实现完整的PostgreSQL迁移脚本：

**UP迁移脚本要求**：
1. 创建task_documents、document_versions、document_logs三个表
2. 添加所有必要的索引和约束
3. 创建自动更新时间戳的触发器
4. 创建版本控制的触发器函数
5. 设置适当的表权限和序列权限

**DOWN迁移脚本要求**：
1. 安全地删除触发器和函数
2. 删除索引和表（按依赖关系逆序）
3. 撤销权限设置
4. 确保回滚的完整性

**技术规范**：
- 文件命名：006_add_task_documents_tables.sql (UP) 和 006_add_task_documents_tables_down.sql (DOWN)
- 包含详细的注释说明
- 错误处理：使用IF EXISTS和IF NOT EXISTS
- 性能考虑：合理的索引策略
- 安全考虑：适当的权限控制

请生成两个完整的SQL迁移文件，确保可以安全地执行和回滚。
```

#### Prompt 307-05: 文档服务层开发
```
请实现Go语言的文档服务层(DocumentService)，提供完整的文档管理功能：

**核心功能要求**：
1. **文件上传和存储**：
   - 接收multipart文件和base64内容
   - 文件类型和大小验证
   - 生成唯一文件路径和名称
   - 计算文件校验和（SHA-256）

2. **版本控制管理**：
   - 自动版本号递增
   - 历史版本文件保留
   - 版本回滚和比较功能

3. **文件检索和查询**：
   - 根据任务ID获取文档列表
   - 支持分页、排序、筛选
   - 文档统计和聚合查询

4. **数据完整性保障**：
   - 数据库事务管理
   - 文件和数据库记录的一致性
   - 错误时的清理机制

**技术规范**：
- 使用GORM进行数据库操作
- 结构体定义要包含JSON标签
- 完整的错误处理和日志记录
- 单元测试覆盖主要功能
- 文件名：document_service.go

请实现DocumentService结构体和所需的方法，确保代码质量和可维护性。
```

#### Prompt 307-06: 手工上传处理器开发
```
基于DocumentService，请实现Gin框架的手工上传HTTP处理器：

**功能要求**：
1. **ManualUploadDocument处理器**：
   - 处理multipart/form-data请求
   - 支持单文件和批量文件上传
   - 文件大小和类型验证
   - 上传进度支持（通过响应头）

2. **参数验证和错误处理**：
   - 项目ID和任务ID验证
   - 用户认证和权限检查
   - 文件格式和大小限制（.md, .pdf, .txt, 最大10MB）
   - 详细的错误信息返回

3. **响应格式**：
   - 统一的JSON响应格式
   - 包含上传文件的详细信息
   - 成功/失败状态和消息

4. **操作审计**：
   - 记录上传操作日志
   - 包含用户信息、IP地址、User-Agent
   - 操作结果和错误信息

**技术规范**：
- 使用Gin的Context进行参数获取
- 从JWT中获取用户信息
- 统一的错误处理中间件
- 合理的HTTP状态码使用
- 文件名：task_document_handler.go (扩展现有文件)

请实现完整的处理器方法，确保安全性和稳定性。
```

#### Prompt 307-07: API上传处理器开发
```
请实现API上传处理器，支持程序化的文档上传：

**功能要求**：
1. **APIUploadDocument处理器**：
   - 接收JSON格式请求
   - 支持base64编码的文件内容
   - 文件元数据设置（文件名、MIME类型、描述）
   - 内容解码和验证

2. **请求结构体**：
   ```go
   type UploadDocumentRequest struct {
       FileName    string `json:"file_name" binding:"required"`
       Content     string `json:"content" binding:"required"`    // base64 encoded
       MimeType    string `json:"mime_type"`
       Description string `json:"description"`
   }
   ```

3. **验证和处理逻辑**：
   - base64内容解码和验证
   - 文件扩展名和MIME类型匹配检查
   - 内容大小限制检查
   - 恶意内容检测（基础）

4. **集成现有系统**：
   - 使用DocumentService进行文件存储
   - 统一的错误处理和响应格式
   - 完整的操作日志记录

**技术规范**：
- JSON请求体验证使用binding标签
- base64解码错误处理
- 与手工上传使用相同的存储逻辑
- 支持API密钥或JWT认证
- 详细的API文档注释

请实现APIUploadDocument方法，确保与现有系统的兼容性。
```

#### Prompt 307-08: 文档下载处理器开发
```
请实现文档下载处理器，支持Markdown和PDF格式导出：

**功能要求**：
1. **DownloadTaskMarkdown处理器**：
   - 整合任务基本信息和描述
   - 包含所有上传的附件信息
   - 生成格式化的Markdown内容
   - 支持文件内容嵌入（对于文本文件）

2. **DownloadTaskPDF处理器**：
   - 将Markdown内容转换为PDF
   - 支持中文字体和排版
   - 包含页眉页脚信息
   - 自定义样式和模板

3. **内容生成逻辑**：
   - 任务信息模板化
   - 附件列表格式化
   - 文档版本信息包含
   - 导出时间戳添加

4. **文件响应处理**：
   - 适当的Content-Type设置
   - Content-Disposition下载头
   - 文件名生成规则
   - 缓存控制策略

**技术实现**：
- Markdown生成使用字符串模板
- PDF转换可以使用简化实现或集成第三方库
- 支持流式响应减少内存占用
- 错误处理和降级策略

**文件结构示例**：
```markdown
# 任务文档导出

## 任务信息
- 任务ID: 123
- 标题: xxx
- 创建时间: xxx

## 任务描述
[任务描述内容]

## 附件文档
### 文档1.md
- 大小: 1KB
- 上传时间: xxx
- 内容: [文档内容]
```

请实现两个下载处理器，确保生成的文档格式规范和用户友好。
```

### 阶段3 Prompts: 前端用户界面

#### Prompt 307-09: 任务文档服务扩展
```
请扩展现有的taskDocumentService.ts，添加Task 307所需的新功能：

**现有服务分析**：
当前service包含基础的get、save、delete方法，需要在此基础上添加上传下载功能。

**新增功能要求**：
1. **uploadDocument方法**：
   - 支持File对象上传
   - 进度回调支持
   - FormData构建和发送
   - 错误处理和重试机制

2. **uploadDocumentAPI方法**：
   - 支持文件名和base64内容上传
   - MIME类型自动推断
   - JSON格式请求构建

3. **uploadMultipleDocuments方法**：
   - 批量文件上传
   - 单个文件进度追踪
   - 失败文件跳过继续处理

4. **下载相关方法**：
   - downloadTaskMarkdown: 下载MD格式
   - downloadTaskPDF: 下载PDF格式
   - triggerDownload: 浏览器下载触发

5. **辅助工具方法**：
   - fileToBase64: 文件转base64
   - validateFile: 文件验证
   - formatFileSize: 大小格式化
   - getMimeTypeFromFileName: MIME类型推断

**TypeScript类型定义**：
```typescript
interface UploadedDocumentInfo {
  id?: number;
  file_name: string;
  original_name: string;
  file_size: number;
  mime_type: string;
  upload_type: 'manual' | 'api';
  uploaded_at: string;
}

interface UploadProgressCallback {
  (progress: number, loaded: number, total: number): void;
}
```

**技术要求**：
- 保持与现有代码风格一致
- 完整的错误处理和用户提示
- axios进度事件处理
- Promise-based异步处理
- 文件名：taskDocumentService.ts (扩展现有文件)

请在现有service基础上添加新功能，确保向后兼容。
```

#### Prompt 307-10: 文档上传组件开发
```
请开发TaskDocumentUploader React组件，提供完整的文档上传功能：

**组件功能要求**：
1. **拖拽上传界面**：
   - 使用Ant Design的Upload.Dragger组件
   - 支持点击选择和拖拽上传
   - 视觉反馈和状态提示

2. **文件管理功能**：
   - 文件列表显示和预览
   - 单个文件删除和重新选择
   - 批量操作支持

3. **上传进度显示**：
   - 单文件上传进度条
   - 批量上传整体进度
   - 成功/失败状态展示

4. **API上传模态框**：
   - 文件名输入和验证
   - 文本内容编辑器
   - MIME类型自动推断

5. **文件验证和错误处理**：
   - 支持格式：.md, .pdf, .txt
   - 大小限制：单文件10MB
   - 清晰的错误提示信息

**组件Props接口**：
```typescript
interface TaskDocumentUploaderProps {
  projectId: number;
  taskId: number;
  onUploadSuccess?: () => void;
  onUploadError?: (error: string) => void;
}
```

**UI设计要求**：
- 使用Ant Design组件库
- 响应式设计，移动端友好
- 清晰的视觉层次和操作流程
- 支持暗色主题

**状态管理**：
- 使用React Hooks管理组件状态
- 文件列表、上传进度、错误信息
- 加载状态和禁用状态处理

请实现完整的上传组件，确保用户体验流畅和直观。
```

#### Prompt 307-11: 文档管理组件开发
```
请开发TaskDocumentManager React组件，提供完整的文档管理界面：

**组件功能要求**：
1. **文档列表展示**：
   - 使用Ant Design的List或Table组件
   - 显示文件信息：名称、大小、类型、上传时间
   - 文件类型图标和状态标识

2. **操作功能菜单**：
   - 下载文档文件
   - 删除文档（软删除）
   - 查看文档详情
   - 版本历史查看

3. **统计信息展示**：
   - 文档总数和总大小
   - 按类型分组统计
   - 上传方式统计

4. **集成上传功能**：
   - 内嵌TaskDocumentUploader组件
   - 支持快捷上传操作
   - 上传后自动刷新列表

5. **搜索和筛选**：
   - 按文件名搜索
   - 按文件类型筛选
   - 按上传时间排序

**组件Props接口**：
```typescript
interface TaskDocumentManagerProps {
  projectId: number;
  taskId: number;
  visible?: boolean;
  onClose?: () => void;
  mode?: 'embedded' | 'modal';
}
```

**布局设计**：
- Tab布局：上传 | 管理 | 统计
- Card布局展示不同功能区域
- 响应式栅格系统

**数据流管理**：
- 使用useTaskDocuments Hook管理状态
- 自动数据刷新和同步
- 乐观更新用户界面

**用户体验优化**：
- 加载骨架屏
- 空状态提示
- 操作确认对话框
- 批量操作支持

请实现功能完整的文档管理组件，确保操作便捷和信息清晰。
```

#### Prompt 307-12: 文档小部件组件开发
```
请开发TaskDocumentWidget轻量级组件，用于任务卡片和详情页快速集成：

**组件设计理念**：
- 轻量级：最小化资源占用
- 多模式：适配不同使用场景
- 快捷操作：一键上传和管理

**显示模式**：
1. **紧凑模式 (compact=true)**：
   - 用于任务卡片
   - 显示文档数量徽章
   - 快速上传按钮
   - 更多操作下拉菜单

2. **完整模式 (compact=false)**：
   - 用于任务详情页
   - 显示详细统计信息
   - 完整的操作按钮组
   - 支持内嵌列表展示

**功能要求**：
1. **快速信息展示**：
   - 文档数量badge显示
   - 总大小和类型分布
   - 最近上传时间

2. **快捷操作支持**：
   - 一键上传（隐藏的input文件选择）
   - 快速下载（MD/PDF）
   - 打开完整管理器

3. **状态管理**：
   - 使用useTaskDocuments Hook
   - 自动数据加载和刷新
   - 错误状态处理

**组件Props接口**：
```typescript
interface TaskDocumentWidgetProps {
  projectId: number;
  taskId: number;
  compact?: boolean;
  showTitle?: boolean;
}
```

**样式要求**：
- 与现有任务组件风格一致
- 支持主题切换
- 响应式适配
- 加载和禁用状态样式

**集成方式**：
- 可以直接嵌入现有任务组件
- 通过Modal方式打开完整管理器
- 支持事件回调通知父组件

请实现灵活适配的文档小部件组件，确保集成简单和使用便捷。
```

#### Prompt 307-13: 文档管理Hook开发
```
请开发useTaskDocuments自定义React Hook，提供完整的文档状态管理：

**Hook功能要求**：
1. **状态管理**：
   - documents: 文档列表数据
   - loading: 加载状态
   - uploading: 上传状态
   - uploadProgress: 上传进度数组
   - error: 错误信息

2. **操作方法**：
   - loadDocuments: 加载文档列表
   - uploadDocument: 单文件上传
   - uploadMultipleDocuments: 批量上传
   - uploadDocumentAPI: API上传
   - downloadMarkdown/PDF: 下载操作
   - deleteDocument: 删除文档
   - refreshDocuments: 刷新数据

3. **工具方法**：
   - getTotalSize: 计算总大小
   - getDocumentsByType: 按类型筛选
   - getDocumentStats: 获取统计信息

**Hook接口设计**：
```typescript
interface UseTaskDocumentsOptions {
  projectId: number;
  taskId: number;
  autoLoad?: boolean;
}

interface UseTaskDocumentsReturn {
  // State
  documents: UploadedDocumentInfo[];
  loading: boolean;
  uploading: boolean;
  uploadProgress: UploadProgress[];
  error: string | null;
  
  // Actions
  loadDocuments: () => Promise<void>;
  uploadDocument: (file: File, onProgress?: (progress: number) => void) => Promise<UploadedDocumentInfo>;
  // ... 其他方法
  
  // Utilities
  getTotalSize: () => number;
  getDocumentStats: () => DocumentStats;
}
```

**技术实现要求**：
- 使用React Hooks (useState, useEffect, useCallback)
- 合理的依赖数组避免不必要的重渲染
- 错误边界处理和用户友好提示
- 内存泄漏防护 (组件卸载时清理)
- 数据缓存和去重处理

**用户体验优化**：
- 乐观更新：操作前先更新UI
- 自动重试：网络错误时的重试机制
- 状态同步：多个组件间的数据一致性
- 性能优化：大量数据的虚拟化处理

请实现功能完整的自定义Hook，确保组件间的状态管理高效和稳定。
```

### 阶段4 Prompts: 集成测试优化

#### Prompt 307-14: 功能集成测试
```
请设计和实施Task 307的完整功能集成测试：

**测试范围和目标**：
1. **端到端功能测试**：验证从前端操作到后端存储的完整流程
2. **跨浏览器兼容性测试**：Chrome、Firefox、Safari、Edge
3. **用户场景测试**：模拟真实用户的操作流程
4. **边界条件测试**：极限情况和异常处理

**测试用例设计**：
1. **文档上传测试**：
   - 手工上传：单文件、多文件、大文件、不支持格式
   - API上传：正确格式、错误格式、无效base64
   - 批量上传：部分成功、全部失败、网络中断

2. **文档下载测试**：
   - Markdown导出：包含附件、空任务、大量文档
   - PDF生成：中文支持、特殊字符、样式渲染
   - 文件命名：时间戳、特殊字符处理

3. **权限和安全测试**：
   - 用户认证：有效token、过期token、无效token
   - 访问控制：跨项目访问、跨任务访问
   - 文件安全：路径遍历、恶意文件上传

4. **性能测试**：
   - 大文件上传：10MB文件处理时间
   - 并发上传：多用户同时上传
   - 列表加载：大量文档的分页性能

**测试工具和方法**：
- 前端：Jest + React Testing Library
- API测试：Postman + Newman
- 端到端：Playwright或Cypress
- 性能测试：Apache Bench或Artillery

**测试数据准备**：
- 准备各种格式和大小的测试文件
- 创建测试用户和项目数据
- 模拟不同的网络环境

**验收标准**：
- 所有核心功能测试通过率100%
- 边界条件处理正确率95%以上
- 跨浏览器兼容性问题0个
- 性能指标达到设计要求

请制定详细的测试计划和执行方案，确保系统质量和稳定性。
```

#### Prompt 307-15: 性能优化和错误处理
```
请对Task 307进行全面的性能优化和错误处理完善：

**性能优化目标**：
1. **前端性能优化**：
   - 组件渲染优化：React.memo、useMemo、useCallback
   - 列表虚拟化：大量文档的分页和懒加载
   - 文件上传优化：分块上传、断点续传
   - 缓存策略：API响应缓存、组件状态缓存

2. **后端性能优化**：
   - 数据库查询优化：索引优化、查询计划分析
   - 文件IO优化：异步处理、并发控制
   - 内存管理：大文件处理的流式读写
   - 缓存机制：Redis缓存热点数据

3. **网络传输优化**：
   - 文件压缩：gzip压缩传输
   - CDN加速：静态资源分发
   - HTTP/2支持：多路复用和服务器推送
   - 带宽自适应：根据网络情况调整策略

**错误处理完善**：
1. **前端错误处理**：
   - 网络错误：重试机制、降级处理
   - 文件错误：格式验证、大小检查、权限提示
   - 用户错误：操作指导、友好提示
   - 系统错误：错误边界、日志上报

2. **后端错误处理**：
   - 参数验证：详细的错误信息返回
   - 业务错误：状态码标准化、错误分类
   - 系统错误：日志记录、监控告警
   - 数据错误：事务回滚、数据一致性

3. **用户体验优化**：
   - 加载状态：骨架屏、进度指示
   - 操作反馈：成功提示、确认对话框
   - 错误恢复：自动重试、手动恢复选项
   - 性能感知：预加载、缓存显示

**监控和诊断**：
- 性能监控：响应时间、吞吐量、错误率
- 用户行为分析：操作路径、错误频率
- 系统健康检查：资源使用、服务状态
- 日志分析：错误模式、性能瓶颈

**技术实现要求**：
- 使用性能分析工具识别瓶颈
- 实施渐进式优化策略
- 保持功能完整性和稳定性
- 文档化优化措施和配置参数

请制定系统的性能优化方案和错误处理策略，确保生产环境的稳定运行。
```

### 阶段5 Prompts: 部署和文档

#### Prompt 307-16: 生产环境部署和文档编写
```
请准备Task 307功能的生产环境部署和完整文档：

**部署准备工作**：
1. **环境配置**：
   - 数据库迁移脚本执行指南
   - 环境变量配置说明
   - Docker容器配置更新
   - Nginx反向代理配置

2. **依赖和权限**：
   - 文件存储目录创建和权限设置
   - PostgreSQL用户权限配置
   - 系统依赖包安装说明
   - 防火墙和网络配置

3. **配置管理**：
   - 生产环境配置文件模板
   - 敏感信息管理（密钥、token）
   - 日志配置和轮转策略
   - 监控和告警配置

**文档编写要求**：
1. **用户使用文档**：
   - 功能介绍和使用场景
   - 操作步骤和界面说明
   - 常见问题和解决方案
   - 最佳实践建议

2. **API集成文档**：
   - 完整的API接口说明
   - 请求参数和响应格式
   - 认证授权机制
   - 代码示例和SDK使用

3. **管理员文档**：
   - 系统架构和技术栈说明
   - 部署和升级指南
   - 配置参数详解
   - 故障排除手册

4. **开发者文档**：
   - 代码结构和设计模式
   - 扩展开发指南
   - 测试规范和流程
   - 贡献指南和代码规范

**文档格式和组织**：
- 使用Markdown格式编写
- 清晰的目录结构和导航
- 丰富的示例和截图
- 版本控制和定期更新

**部署验证**：
- 部署流程测试和验证
- 功能回归测试执行
- 性能基准测试
- 安全检查和漏洞扫描

**交付成果**：
1. 部署指南文档（DEPLOYMENT.md）
2. 用户手册文档（USER_MANUAL.md）
3. API文档（API_REFERENCE.md）
4. 故障排除指南（TROUBLESHOOTING.md）
5. 演示材料和培训PPT

请编写完整的部署方案和文档体系，确保系统能够顺利上线和维护。
```

## 📊 开发计划总结

### 时间分配
- **阶段1 (基础架构)**: 8小时 - 2天
- **阶段2 (后端开发)**: 24小时 - 6天
- **阶段3 (前端开发)**: 20小时 - 5天
- **阶段4 (测试优化)**: 8小时 - 2天
- **阶段5 (部署文档)**: 4小时 - 1天
- **总计**: 64小时 - 16工作日

### 优先级分布
- **高优先级任务**: 12个 (75%)
- **中优先级任务**: 4个 (25%)

### 依赖关系
- 严格按阶段顺序执行
- 同阶段内的任务可并行开发
- 关键路径：数据库设计 → 服务层开发 → API开发 → 前端组件

### 风险控制
- 每个阶段设置里程碑检查
- 预留20%缓冲时间处理问题
- 关键功能提前验证可行性
- 持续集成和测试保障质量

---

**文档创建时间**: 2025-01-04  
**计划执行周期**: 16个工作日  
**预估总投入**: 64工时