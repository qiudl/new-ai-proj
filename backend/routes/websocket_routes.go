package routes

// import (
// 	"github.com/gin-gonic/gin"
// )

// RegisterWebSocketRoutes registers WebSocket routes - DISABLED
// func RegisterWebSocketRoutes(router *gin.Engine, app ApplicationInterface) {
// 	// WebSocket endpoint (no auth middleware, auth handled in handler)
// 	router.GET("/ws", app.GetWebSocketHandler())
// }
