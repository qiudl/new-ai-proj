package routes

import (
	"github.com/gin-gonic/gin"
)

// RegisterPermissionMonitoringRoutes registers permission monitoring and analytics routes (disabled stub)
// The advanced monitoring features are currently disabled until supporting interfaces are wired.
func RegisterPermissionMonitoringRoutes(authorized *gin.RouterGroup, app ApplicationInterface) {
	// No-op stub to keep build stable.
}
