package routes

import (
	"github.com/gin-gonic/gin"
)

// RegisterUserStatsRoutes 注册用户统计相关路由（简化版，暂时禁用）
func RegisterUserStatsRoutes(authorized *gin.RouterGroup, app ApplicationInterface) {
	// 暂时禁用用户统计路由以便快速测试角色权限API
}
