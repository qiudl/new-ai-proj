# Prisma environment for Docker compose prisma service
# This should mirror .env.docker DB_SOURCE so Prisma sees the same database
DATABASE_URL=postgresql://user:password@db:5432/main_db?schema=public&sslmode=disable

