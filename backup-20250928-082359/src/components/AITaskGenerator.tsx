import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

/**
 * AI任务生成器组件
 * 临时占位符组件
 */
const AITaskGenerator: React.FC = () => {
  return (
    <Card>
      <Title level={4}>AI任务生成器</Title>
      <p>此组件正在开发中...</p>
    </Card>
  );
};

export default AITaskGenerator;