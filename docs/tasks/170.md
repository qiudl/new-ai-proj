任务文档系统分析与修复完成

  模板问题已完全修复：
  1. ✅ 删除危险的模板生成功能：
    - 移除了 generateDefaultTemplate 函数（backend/handlers/task_document_handler.go:46）
    - 修改 getDocumentGin 函数，文件不存在时返回空内容而非模板（第109-111行）
    - 删除了 GetTaskDocumentDefaultTemplate 函数（backend/models/task.go:302-303）
  2. ✅ 文档保存功能正常：
    - API路由配置正确：PUT /projects/:id/tasks/:taskId/document
    - 保存逻辑完整：saveDocumentGin 函数会创建目录并写入文件
    - 前端编辑器集成正确：使用PUT请求保存文档
    - 实际测试显示：文档确实会被保存到 ./docs/tasks/ 目录
  3. ✅ 任务文档设计分析：
    - 存储方式：使用文件系统存储（非数据库），每个任务一个.md文件
    - 版本管理：不支持版本管理，只保存最新版本
    - 删除机制：支持软删除，任务删除时文档文件保留（只有任务硬删除时才会删除文档）
    - 路径规则：./docs/tasks/{taskId}.md

  为什么用户感觉文档没有生成：
  问题可能出现在以下情况：
  - 模板覆盖导致之前保存的内容丢失，给用户造成"保存后没有文档"的错觉
  - 前端缓存问题导致看不到最新保存的内容

  文档内容恢复建议：
  由于不支持版本管理，已丢失的内容无法恢复。但现在模板问题已修复，新保存的内容不会再丢失。

  当前状态：
  - ✅ 模板覆盖问题已彻底解决
  - ✅ 文档保存功能正常工作
  - ✅ 系统已恢复到安全的MVP状态
  - ❌ 历史丢失的内容无法恢复（因为没有版本控制）

  用户现在可以安全地使用任务文档编辑功能，不会再出现内容丢失的问题。