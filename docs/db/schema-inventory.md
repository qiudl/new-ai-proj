# Schema 盘点清单（草稿）

> 关联任务：574 M1-1 现状盘点与对齐准则（仓库暂存版；任务系统接口恢复后将自动关联）

## 范围
- 对现有 Postgres（本地以 Docker 运行、CI/生产同源）进行结构盘点

## 盘点对象清单
- 表（名称/用途/行数级别/是否分区）
- 列（名称/类型/默认值/是否可空/备注）
- 约束（主键/唯一/外键/检查/默认）
- 索引（类型/覆盖列/是否唯一/估算大小）
- 触发器/函数/扩展
- 视图与物化视图

## 发现与问题（记录样例）
- 潜在风险点：大字段、无索引外键、复合主键、命名不一致等

## 产出
- 本文档（Markdown/CSV）
- （可选）结构关系图（ER 图）

## 连接与环境说明
- 开发/测试遵循：Docker Postgres；通过环境变量注入 DATABASE_URL
- CI 在 Jenkins Docker agent 中执行 prisma generate/migrate/test

